name: Build and Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      version:
        description: 'Version for the release (e.g., 1.0.0)'
        required: true

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm install

      - name: Build for Chrome
        run: npm run build:chrome
        env:
          NODE_ENV: production

      - name: Build for Firefox
        run: npm run build:firefox
        env:
          NODE_ENV: production

      - name: Get version
        id: version
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            echo "version=${{ github.event.inputs.version }}" >> $GITHUB_OUTPUT
          else
            echo "version=${GITHUB_REF#refs/tags/v}" >> $GITHUB_OUTPUT
          fi

      - name: Create Chrome/Edge zip
        run: |
          cd dist-chrome
          zip -r ../authvault-chrome-${{ steps.version.outputs.version }}.zip .

      - name: Create Firefox zip (unsigned)
        run: |
          cd dist-firefox
          zip -r ../authvault-firefox-${{ steps.version.outputs.version }}.zip .

      - name: Sign Firefox extension
        if: ${{ env.AMO_JWT_ISSUER != '' }}
        env:
          AMO_JWT_ISSUER: ${{ secrets.AMO_JWT_ISSUER }}
          AMO_JWT_SECRET: ${{ secrets.AMO_JWT_SECRET }}
        run: |
          npm install -g web-ext
          web-ext sign --source-dir=dist-firefox --channel=unlisted --api-key=$AMO_JWT_ISSUER --api-secret=$AMO_JWT_SECRET
          mv web-ext-artifacts/*.xpi authvault-firefox-${{ steps.version.outputs.version }}.xpi

      - name: Generate Release Notes
        id: release_notes
        run: |
          PREV_TAG=$(git describe --tags --abbrev=0 HEAD^ 2>/dev/null || echo "")

          echo "notes<<EOF" >> $GITHUB_OUTPUT

          echo "## AuthVault v${{ steps.version.outputs.version }}" >> $GITHUB_OUTPUT
          echo "" >> $GITHUB_OUTPUT
          echo "### Downloads" >> $GITHUB_OUTPUT
          echo "- **Chrome / Edge**: \`authvault-chrome-${{ steps.version.outputs.version }}.zip\` — load as unpacked extension or upload to Edge Add-ons" >> $GITHUB_OUTPUT
          echo "- **Firefox**: \`authvault-firefox-${{ steps.version.outputs.version }}.zip\` (unsigned) or \`.xpi\` (signed, if available)" >> $GITHUB_OUTPUT
          echo "" >> $GITHUB_OUTPUT
          echo "### Changes" >> $GITHUB_OUTPUT

          if [ -n "$PREV_TAG" ]; then
            git log $PREV_TAG..HEAD --pretty=format:"- %s" | head -30 >> $GITHUB_OUTPUT
          else
            git log --pretty=format:"- %s" -10 >> $GITHUB_OUTPUT
          fi

          echo "" >> $GITHUB_OUTPUT
          echo "" >> $GITHUB_OUTPUT

          if [ -n "$PREV_TAG" ]; then
            echo "**Full Changelog**: https://github.com/${{ github.repository }}/compare/${PREV_TAG}...v${{ steps.version.outputs.version }}" >> $GITHUB_OUTPUT
          fi

          echo "EOF" >> $GITHUB_OUTPUT

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          name: AuthVault v${{ steps.version.outputs.version }}
          tag_name: v${{ steps.version.outputs.version }}
          body: ${{ steps.release_notes.outputs.notes }}
          fail_on_unmatched_files: false
          files: |
            authvault-chrome-${{ steps.version.outputs.version }}.zip
            authvault-firefox-${{ steps.version.outputs.version }}.zip
            authvault-firefox-${{ steps.version.outputs.version }}.xpi
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
